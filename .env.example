# Example environment variables for Astro Bio Navigator Server
# Copy this file to .env and fill in your keys before running the server

# OpenRouter API key (required for article summarization)
OPENROUTER_API_KEY=

# Optional: port to run the server on (default: 3000)
PORT=3000

# Optional: if you have a Semantic Scholar API key (not required for public endpoints)
SEMANTIC_SCHOLAR_API_KEY=

# Supabase (optional - server side)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
